class EventHandler:
	def process(event_list, ev ,event_type):
		if event_type == 'arrival':
			if ev.coreId == -2:
				#code for drop
			elif ev.coreId == -1:
				#request in sq
			else:
				#request in cq
				#quantum done
				#gererate 
				event_list.insert()
			
			
			
		if event_type == 'departure':
			
			
		if event_type == 'quantumDone':
			
			
		if event_type == 'switchingDone':
		
		
	